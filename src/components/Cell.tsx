import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Cell = ({style}:any) => {
  return (
    <View style={style}>
    </View>
  )
}

export default Cell

const styles = StyleSheet.create({})