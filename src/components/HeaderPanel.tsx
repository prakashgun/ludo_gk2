import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const HeaderPanel = ({ style }: any) => {
  return (
    <View style={style}>
      <Text>HeaderPanel</Text>
    </View>
  )
}

export default HeaderPanel

const styles = StyleSheet.create({})